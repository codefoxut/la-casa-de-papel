```
Given an array of integers, return true if tthe following conditions are fulfilled
 # length of the array is bigger than or equal to 3.
 # theree exists some index i such that:
    a[0] < a[1] < ... < a[i]
    a[i] > a[i+1] > ... > a[a.size-1]
```